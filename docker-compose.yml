services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: pandajobs
    container_name: pandajobs
    env_file:
      - .env.dev
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
